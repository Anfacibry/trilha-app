import 'package:flutter/material.dart';

class ContainerRoxo extends StatelessWidget {
  const ContainerRoxo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple,
    );
  }
}
