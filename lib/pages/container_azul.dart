import 'package:flutter/material.dart';

class ContainerAzul extends StatelessWidget {
  const ContainerAzul({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
